# 旅行足迹（Travel Footprint）产品研发计划书

> 版本：v1.0 ｜ 编写角色：产品经理 ｜ 日期：2026-08-15
> 状态：待评审 ｜ 本文档为「脚手架 → 可面向用户的产品」的顶层研发规划

---

## 目录

1. [项目现状盘点](#1-项目现状盘点)
2. [产品定位与目标用户](#2-产品定位与目标用户)
3. [竞品与市场分析](#3-竞品与市场分析)
4. [产品功能架构（To-Be）](#4-产品功能架构to-be)
5. [AI 模块专项设计（核心新增）](#5-ai-模块专项设计核心新增)
6. [分阶段研发路线图](#6-分阶段研发路线图)
7. [技术架构演进](#7-技术架构演进)
8. [验收标准与 KPI](#8-验收标准与-kpi)
9. [风险与对策](#9-风险与对策)
10. [下一步行动](#10-下一步行动)

---

## 1. 项目现状盘点

### 1.1 项目是什么

**旅行足迹（Travel Footprint）** 是一个个人旅行记录 Web 应用，核心价值是「记录你去过的每一座城市与景点，可视化你的旅行人生」。当前形态为**纯前端 SPA**：Vue 3 + TypeScript + Vite 5 + Pinia + Leaflet。

### 1.2 已有功能（脚手架完成度盘点）

| 模块 | 文件 | 完成度 | 说明 |
|------|------|--------|------|
| 足迹地图 | `ChinaMap.vue` / `MapView.vue` | ✅ 基本完成 | 省份着色、城市圆点、年份/同伴筛选联动、Popup |
| 景点打卡 | `ScenicView.vue` / `scenicStore.ts` | ✅ 基本完成 | 4A/5A 景点库、打卡/心愿单、省份/等级/状态筛选 |
| 旅行回忆 | `MemoryList/Detail/FormView.vue` | ✅ 基本完成 | 图文日记、多图灯箱、标签筛选、CRUD |
| 数据管理 | `DataManageView.vue` / `exportImport.ts` | ✅ 基本完成 | JSON 备份/恢复、手动添加城市与景点 |
| 数据层 | 3 个 Pinia store + `storage.ts` | ⚠️ 有隐患 | localStorage 直写、`patchState` 存在理论竞态、版本无迁移 |
| 首页 | `HomeView.vue` | ✅ 基本完成 | Hero + 统计 + 近期回忆 |
| 部署 | GitHub Actions + Pages | ✅ 已配置 | 尚未验证生产构建 |

### 1.3 与「可面向用户产品」的差距（关键缺口）

| # | 缺口 | 影响 | 严重度 |
|---|------|------|--------|
| 1 | **数据只存浏览器 localStorage** | 清缓存/换设备即丢数据，无账号无云同步 | 🔴 致命 |
| 2 | **无用户体系** | 无法区分用户、无法多设备、无法做分享 | 🔴 致命 |
| 3 | **无 AI 能力** | 缺失差异化竞争力（用户明确要求拓展） | 🔴 高 |
| 4 | **GeoJSON 依赖阿里 DataV CDN** | 域名不稳定/被墙风险，地图可能白屏 | 🟡 中 |
| 5 | **坐标需手动填写** | 添加城市/景点体验门槛高，易填错 | 🟡 中 |
| 6 | **无 PWA/离线化** | 移动端体验差，与「记录型产品」定位不符 | 🟡 中 |
| 7 | **无埋点/反馈通道** | 无法衡量产品健康度、无法收集用户反馈 | 🟢 低 |
| 8 | **Element Plus 已安装但未使用** | 依赖冗余，UI 一致性靠手写 CSS | 🟢 低 |
| 9 | **生产构建失败**（已验证） | `npm run build` 报 6 处 TS 错误，无法产出可部署产物 | 🔴 致命 |

### 1.4 构建基线问题明细（2026-08-15 实测）

```
src/components/ChinaMap.vue(60,6):   'this' 隐式 any（mouseover 事件处理器）
src/components/ChinaMap.vue(63,6):   'this' 隐式 any（mouseout 事件处理器）
src/components/ScenicSpotCard.vue(4,7):  'props' 声明未使用
src/router/index.ts(59,18):         scrollBehavior 参数 'to' 未使用
src/router/index.ts(59,22):         scrollBehavior 参数 'from' 未使用
src/views/MemoryFormView.vue(36,9): 'string | number' 不能赋给 'string'
```

> 影响：`vue-tsc -b` 类型检查失败 → CI 部署（deploy.yml）将直接失败，**当前仓库无法通过 GitHub Actions 发布**。此问题必须在 Phase 4 开工第一天修复，并建立「构建必须通过」的提交门槛。

---

## 2. 产品定位与目标用户

### 2.1 定位（已确认方向）

> **分阶段演进：先打磨为高质量个人工具，后开放轻社交。**

- **第一阶段（本次计划核心）**：把「旅行足迹」打磨成一个**数据安全、体验流畅、AI 加持**的个人旅行记录工具 —— 用户可放心长期使用、数据永不丢失。
- **第二阶段（预留）**：开放「足迹分享」轻社交 —— 生成分享卡片/公开页，让朋友看到你的足迹，形成自传播。

### 2.2 目标用户画像

| 画像 | 特征 | 核心诉求 |
|------|------|----------|
| **旅行记录爱好者**（主力） | 25-40 岁，喜欢旅行、拍照，想记录足迹但不擅长写长文 | 快速记录、可视化成果、数据不丢 |
| **效率型旅行者** | 出行前做攻略，收藏心愿单景点 | 一键生成行程、规划助手 |
| **数据控/复盘型用户** | 每年复盘去过哪、花了多少钱 | 年度报告、数据洞察、统计可视化 |

### 2.3 产品价值主张（一句话）

> **「你的每一次出发，都值得被记住。旅行足迹帮你记录、规划、复盘每一段旅程，并用 AI 让记录更轻松、让旅行更有灵感。」**

### 2.4 产品原则

1. **数据永远属于用户** —— 云同步 + 本地备份双保险，导出格式开放（JSON/Markdown）。
2. **记录成本趋近于零** —— AI 代填坐标、自动打标签、语音/照片导入。
3. **先做深，再做广** —— 单点功能做到极致，不堆砌半成品功能。
4. **AI 是助手不是主角** —— AI 生成内容必须可编辑、可撤回，始终保留用户控制权。

---

## 3. 竞品与市场分析

### 3.1 市场趋势

在线旅游业正掀起 AI 竞速赛：携程构建覆盖 B-C 两端的 AI 生态网，马蜂窝上线旅游行业超级智能体，AI 行程助手成为用户呼声最高的功能（来源：[界面新闻](https://www.jiemian.com/article/13209951.html)、[动点科技](https://cn.technode.com/post/partner/mafengwo-ai-agent/)、[36氪](https://m.36kr.com/p/3431274545450627)）。同时用户对「AI 旅游助手」仍有省心 vs 添乱的争议，**个性化、懂用户历史数据**是破局点（来源：[工人日报](https://www.workercn.cn/c/2025-09-19/8611527.shtml)）。

### 3.2 竞品格局

| 竞品 | 形态 | 优势 | 对本产品的启示 |
|------|------|------|----------------|
| 携程/去哪儿 AI 助手 | 交易平台内嵌 | 资源（酒店/机票）丰富 | 我们不做交易，做**个人数据沉淀** |
| 马蜂窝 AI Agent | 内容社区 | 攻略内容库庞大 | 我们有**用户自己的足迹数据**，这是独家上下文 |
| Wanderlog 等国外行程规划 | 工具型 | 行程编辑体验成熟 | 行程编辑器的 UX 值得参考 |
| 各平台年度报告（网易云/航旅纵横） | 数据复盘 | 传播性强 | **年度足迹报告**是极佳的分享钩子 |

### 3.3 差异化机会

> 大平台有资源，但**没有用户的私人足迹数据**；个人工具能沉淀数据，但大多没有 AI 能力。**「个人足迹数据 + AI 个性化」** 是旅行足迹的独家生态位 —— AI 规划基于"你真正去过/想去的地方"，AI 复盘基于"你真实的行程开销"，这是任何通用 AI 助手做不到的。

---

## 4. 产品功能架构（To-Be）

### 4.1 信息架构

```
旅行足迹
├── 首页（数据总览 + 快捷入口 + AI 足迹洞察入口）
├── 足迹地图
│   ├── 中国省份着色地图（已访高亮）
│   ├── 城市标记（年份/同伴筛选联动）
│   ├── 轨迹连线（预留：旅行路线动画）
│   └── 世界地图（远期：境外足迹）
├── 景点打卡
│   ├── 4A/5A 景点库（省份/等级/状态筛选）
│   ├── 打卡 / 心愿单 / 自定义景点
│   └── 🆕 AI 景点推荐（基于足迹画像）
├── 旅行回忆
│   ├── 图文日记 CRUD + 标签 + 灯箱
│   └── 🆕 AI 回忆助手（从照片/素材生成初稿）
├── 🆕 AI 旅行助手（新模块，见第 5 章）
│   ├── 行程规划
│   ├── 足迹数据洞察（年度报告/统计解读）
│   └── 智能整理（自动坐标/标签/归类）
├── 账号与云同步（🆕）
│   ├── 登录（邮箱/第三方 OAuth）
│   ├── 云备份 / 多设备同步
│   └── 数据导出（JSON / Markdown）
├── 数据管理（现有）
└── 设置（主题/单位/隐私开关）
```

### 4.2 功能清单（按优先级）

| 优先级 | 功能 | 归属阶段 | 说明 |
|--------|------|----------|------|
| P0 | 云同步 + 账号体系 | Phase 4 | 解决数据丢失致命伤 |
| P0 | AI 行程规划助手 | Phase 5 | 核心差异化能力 |
| P0 | AI 足迹数据洞察（年度报告） | Phase 5 | 传播钩子 + 留存 |
| P0 | 坐标自动解析（城市名→经纬度） | Phase 4 | 降低记录门槛 |
| P1 | AI 智能整理（自动标签/信息补全） | Phase 5 | 降本增效 |
| P1 | GeoJSON 本地化/多源降级 | Phase 4 | 地图稳定性 |
| P1 | PWA 离线化 | Phase 4 | 移动端体验 |
| P1 | 足迹分享卡片（轻社交） | Phase 6（预留） | 自传播 |
| P2 | 轨迹连线动画 | Phase 6（预留） | 视觉亮点 |
| P2 | 世界地图 | 远期 | 境外足迹 |
| P2 | 用户反馈通道 + 埋点 | Phase 6 | 数据驱动迭代 |

---

## 5. AI 模块专项设计（核心新增）

> 这是本次计划的重头戏。基于确认方向：**AI 行程规划助手、AI 足迹数据洞察、AI 智能整理** 三项能力（AI 回忆生成作为 P2 预留）。

### 5.1 模块总览

```
┌─────────────────────────────────────────────────┐
│                 AI 旅行助手（/ai）                 │
├──────────────┬────────────────┬──────────────────┤
│ ① 行程规划    │ ② 足迹数据洞察  │ ③ 智能整理         │
│ 目的地+天数    │ 年度/累计报告    │ 坐标自动解析       │
│ +偏好 → 行程   │ +统计解读       │ 自动标签/信息补全  │
├──────────────┴────────────────┴──────────────────┤
│          统一 AI 网关（前端 SDK 层）                │
│        模型调用：DeepSeek API（经后端代理）          │
└─────────────────────────────────────────────────┘
```

### 5.2 能力①：AI 行程规划助手

#### 5.2.1 用户故事

> 小张想去四川玩 5 天，之前心愿单收藏了「九寨沟、峨眉山、都江堰」。她打开 AI 助手输入「四川 5 天 4 晚，喜欢自然风光，预算 3000」，AI 结合她心愿单里收藏的四川景点 + 已去过的城市 + 她的出行偏好，生成一份**含每日安排、交通建议、预算估算、景点衔接**的行程单。她可以一键把行程中的新景点加入心愿单、把行程存为回忆草稿。

#### 5.2.2 交互流程

```
输入（目的地/天数/风格/预算/同伴） → [生成] → 行程卡片流（按天）
   ├─ 可编辑：改天数 → 重新生成；改某天 → 局部重排
   ├─ 一键操作：加入心愿单 / 保存为回忆草稿 / 复制 Markdown
   └─ 反馈：👍/👎（用于数据积累与 prompt 调优）
```

#### 5.2.3 输入输出规格

| 项 | 规格 |
|----|------|
| 输入 | `destination`（必填）、`days`（1-15）、`style`（自然/人文/美食/亲子/穷游/深度）、`budget`、`companions`、`startDate` |
| 输出 | `DayPlan[]`：`{ day, title, spots: [{name, city, level, duration, tip}], meals?, transport?, budget?, notes? }` |
| 上下文（注入 Prompt） | ① 用户心愿单景点（按目的地省份过滤）② 用户已访城市 ③ 景点库中该省 4A/5A 数据 ④ 用户历史偏好（回忆 tags 统计） |
| 约束 | ① 行程景点优先从用户心愿单/景点库选取，库外景点需标注「新发现」② 每天 ≤ 4 个景点，留机动时间 ③ 预算分项（交通/住宿/门票/餐饮） |

#### 5.2.4 Prompt 策略（示例骨架）

```
你是资深旅行规划师。用户资料：已访城市[..]、心愿单[..]、偏好[..]。
任务：为 {destination} 规划 {days} 天行程，风格 {style}，预算 {budget}。
规则：
1. 优先使用用户心愿单中该地景点，标注⭐；景点库数据可用但未收藏的标注☆；两者都没有的标注✨新发现。
2. 每天不超过4个景点，标注建议停留时长。
3. 输出严格 JSON：{"days": [{ "day":1, "title":"", "spots":[{...}], "budget":{...}, "tips":"" }], "summary":"..."}
4. 若天数与景点数量不匹配，需给出「机动日/备选景点」建议。
5. 结尾用一句话给出行程亮点。
```

> 采用 **function calling / 严格 JSON 输出** 约束，前端直接渲染结构化卡片而非自由文本。

### 5.3 能力②：AI 足迹数据洞察

#### 5.3.1 用户故事

> 年底了，小张打开「我的年度足迹报告」：AI 根据她一年的城市、景点、回忆、开销数据，生成「今年你点亮了 6 座城市，走了 21 天，去过 2 个 5A 景区…你偏爱自然风光，最舍得花钱在住宿上」等个性化解读 + 可视化图表 + 一张可分享的年度总结卡片。

#### 5.3.2 功能规格

| 子功能 | 说明 |
|--------|------|
| 年度报告 | 选择年份 → 城市数/省份数/景点数/回忆数/总天数/总花费 + AI 文字解读 |
| 足迹画像 | 全量数据聚合：最常去省份、城市次数排行、景点类型偏好（从回忆 tags 统计）、同伴分布 |
| 数据解读 | AI 用自然语言总结规律与趋势，生成 3-5 条洞察（如「你 60% 的旅行集中在夏季」）|
| 分享卡片 | 生成 1080×1080 图片（Canvas 绘制 + 导出 PNG），供分享到社交平台 |

#### 5.3.3 数据来源与计算

- **前端聚合**（无需后端）：`visitedCities`、`scenicSpots`、`memories`（含 cost、tags、companions）均由 store 提供，前端 computed 聚合统计 → 喂给 LLM 做解读。
- **LLM 只做「解读」不做「计算」**：所有数字由前端精确统计，LLM 负责风格化叙述，避免幻觉数字。

### 5.4 能力③：AI 智能整理

| 子功能 | 用户痛点 | AI 方案 |
|--------|----------|---------|
| **坐标自动解析** | 手动填 lat/lng 门槛高 | 输入城市/景点名 → 调地理编码（高德/百度免费 API，或 LLM 兜底）→ 自动填充坐标 |
| **自动打标签** | 回忆分类靠手动 | 根据回忆 title/content/城市，LLM 生成 3-5 个 tags，用户确认后写入 |
| **景点信息补全** | 自定义景点只有名字 | LLM 生成 description/type/最佳季节，标注「AI 生成，请核实」 |
| **回忆摘要** | 长文回忆难以回顾 | 卡片视图自动生成 30 字摘要 |

> 原则：**所有 AI 生成内容带「AI」标记 + 一键采纳/拒绝**，杜绝脏数据污染用户资产。

### 5.5 AI 技术方案

#### 5.5.1 模型选型

| 项 | 选择 | 理由 |
|----|------|------|
| 主模型 | **DeepSeek-V3（deepseek-chat）** | 中文能力优秀、价格极低（约 ¥2/百万 tokens 输入）、国内访问稳定 |
| 强推理场景（可选） | deepseek-reasoner | 复杂行程约束求解时可切换 |
| 备用 | 通义千问 / GLM | 降级容灾 |

#### 5.5.2 调用链路（关键：**API Key 绝不进前端**）

```
前端（浏览器）                     后端代理（Supabase Edge Function / Serverless）
┌─────────────┐    HTTPS 请求     ┌──────────────────────────────┐
│ 用户操作     │ ────────────────▶ │ POST /api/ai/plan             │
│ 携带用户 token│                  │ 校验 JWT → 组装 Prompt → 调 DeepSeek │
└─────────────┘ ◀──────────────── └──────────────────────────────┘
       结构化 JSON（带流式返回，SSE）
```

- **为什么用后端代理**：API Key 放前端会直接泄露（浏览器可被任何人查看），且无法做限流/审计/成本控制。
- **Supabase Edge Function**（Deno 运行时）或 Vercel Serverless 均可，随账号体系一并落地（见第 7 章）。

#### 5.5.3 成本估算与风控

| 场景 | 估算 | 说明 |
|------|------|------|
| 行程规划（单次） | ~3-6K tokens 输入 + 1K 输出 ≈ ¥0.01-0.02 | 按 DeepSeek 定价 |
| 年度报告解读（单次） | ~4-8K tokens ≈ ¥0.02 | 前端先聚合再喂数据 |
| 智能整理（每条） | <1K tokens ≈ <¥0.005 | 批量打标签可合并请求 |
| **风控** | 每用户每日限 30 次 / 单次超时 30s / 失败自动降级提示 | Edge Function 侧限流 |

> 结论：**AI 功能即使 1000 个活跃用户，月成本也在百元级**，商业化可行。

#### 5.5.4 隐私与合规

1. **最小化原则**：只把「规划所需」的数据发送给 LLM，不发送用户 ID、联系方式等敏感字段。
2. **明示告知**：AI 功能页面标注「内容由 AI 生成，仅供参考」。
3. **可删除**：用户可在设置中一键清除 AI 使用记录（Prompt 日志留存 ≤ 30 天）。
4. **合规注意**：若后续上线公开分享，需按《生成式人工智能服务管理暂行办法》要求做内容安全过滤（接入内容审核 API）。

### 5.6 AI 模块验收标准

- [ ] `/ai` 页面可完成「目的地+天数+偏好 → 结构化行程卡片」全流程，生成时间 < 30s
- [ ] 行程中的景点可一键「加入心愿单」并写入 scenicStore
- [ ] 年度报告数字与前端统计**完全一致**（LLM 不参与计算，防幻觉）
- [ ] 坐标自动解析：输入「成都」自动填充 30.57/104.07，误差 < 0.1°
- [ ] AI 生成内容全部带「AI」标记，可一键拒绝
- [ ] 无 API Key 泄露风险（前端包中 grep 不到 key）
- [ ] 无网络/API 失败时，页面优雅降级（显示提示，不白屏）

---

## 6. 分阶段研发路线图

> 采用「**数据安全 → AI 差异化 → 轻社交增长**」三段式演进。每阶段结束都有可验收的版本。

### Phase 4：体验夯实 + 数据安全（约 2-3 周）★ 必做

**目标：消除「数据会丢」的致命伤，降低记录门槛，打磨可用性。**

| # | 任务 | 优先级 | 估时 | 说明 |
|---|------|--------|------|------|
| 4.0 | **修复生产构建**（1.4 节 6 处 TS 错误）+ 提交 git 基线 | P0 | 0.5d | 建立「构建必须通过」门槛，解锁 CI 部署 |
| 4.1 | 引入 Supabase：Auth（邮箱 + GitHub OAuth） | P0 | 2d | 账号体系落地 |
| 4.2 | 数据表设计 + 云同步（local-first 同步策略，见 7.3） | P0 | 3d | cities/spots/memories 三表 + profiles |
| 4.3 | 首次登录引导：本地数据一键迁移到云端 | P0 | 1d | 老用户数据不丢 |
| 4.4 | 城市/景点坐标自动解析（高德地理编码 API） | P0 | 1.5d | 替换手动填坐标 |
| 4.5 | GeoJSON 本地打包 + 多源降级（DataV → 本地 → 简化版） | P1 | 1d | 地图稳定性 |
| 4.6 | PWA 化（vite-plugin-pwa + 离线缓存 + 安装提示） | P1 | 2d | 移动端体验 |
| 4.7 | 统一 UI 组件库（接入 Element Plus 或精修 CSS 设计系统） | P2 | 2d | 二选一，避免两套并存 |
| 4.8 | 数据版本迁移机制（storage 增加 schemaVersion + 迁移函数） | P1 | 1d | 修复 1.2 遗留隐患 |

**验收**：新设备登录后数据自动同步；清浏览器缓存数据不丢；添加城市只需输入名称自动带出坐标；无网时核心功能可用。

### Phase 5：AI 模块上线（约 3-4 周）★ 核心差异化

**目标：上线 AI 旅行助手三大能力，形成竞品壁垒。**

| # | 任务 | 优先级 | 估时 | 说明 |
|---|------|--------|------|------|
| 5.1 | 搭建 AI 网关（Edge Function + DeepSeek 接入 + 限流 + 错误降级） | P0 | 2d | 基础设施 |
| 5.2 | `/ai` 页面框架 + 行程规划全流程（输入表单 → 流式生成 → 行程卡片） | P0 | 3d | 能力① |
| 5.3 | 行程卡片操作：加入心愿单 / 存回忆草稿 / 复制 Markdown | P0 | 1.5d | 打通与现有模块 |
| 5.4 | 足迹数据洞察：统计聚合引擎（前端精确计算） | P0 | 2d | 能力②前置 |
| 5.5 | 年度报告页 + AI 解读 + 分享卡片（Canvas 导出 PNG） | P1 | 2.5d | 能力② |
| 5.6 | 智能整理：坐标兜底解析 + 自动标签 + 景点信息补全 | P1 | 2d | 能力③ |
| 5.7 | AI 内容安全过滤（接入审核 API，为分享做准备） | P1 | 1d | 合规 |
| 5.8 | AI 使用埋点 + 反馈（👍/👎） | P2 | 1d | 数据驱动调优 |

**验收**：见 5.6 节 AI 模块验收标准全部通过。

### Phase 6：轻社交 + 增长（约 2-3 周）★ 预留

**目标：开放足迹分享，形成自传播；建立数据驱动迭代闭环。**

| # | 任务 | 优先级 | 估时 | 说明 |
|---|------|--------|------|------|
| 6.1 | 足迹分享卡片/公开页（只读 URL，可选择公开范围） | P0 | 3d | 轻社交 |
| 6.2 | 世界地图（境外足迹，复用现有地图架构） | P1 | 2d | 覆盖更多用户 |
| 6.3 | 轨迹连线动画（城市间路线） | P2 | 1.5d | 视觉亮点 |
| 6.4 | 埋点体系（访问/功能使用/留存）+ 反馈入口 | P1 | 2d | 数据驱动 |
| 6.5 | 内容审核流水线上线（公开内容必经审核） | P0 | 1.5d | 合规 |
| 6.6 | 年度报告年度化运营（每年 12 月底生成入口置顶） | P1 | 1d | 传播峰值 |

**验收**：生成分享链接可公开访问；报告分享带来新用户注册（指标见第 8 章）。

### 远期（Post-Phase 6，另行立项）

- AI 回忆生成（照片→图文初稿，用户未选但建议 P2 评估）
- 移动端 App（Flutter/uni-app 复用后端）
- 行程协作（多人共建行程）
- 景点库扩充至全国 4A/5A 全量 + 数据自动更新

---

## 7. 技术架构演进

### 7.1 演进总览

```
现状（脚手架）             Phase 4（数据安全）         Phase 5+（AI 差异化）
┌──────────────┐       ┌──────────────────┐       ┌─────────────────────┐
│ Vue3 + Pinia │       │ Vue3 + Pinia     │       │ Vue3 + Pinia        │
│ localStorage │ ───▶  │ + Supabase 云同步 │ ───▶  │ + AI 网关（Edge Fn） │
│ 无账号        │       │ + Auth 账号体系   │       │ + 流式生成/分享      │
│ GeoJSON CDN  │       │ + 本地 GeoJSON   │       │ + 埋点体系          │
└──────────────┘       └──────────────────┘       └─────────────────────┘
```

### 7.2 后端选型（已确认：轻量方案）

| 需求 | 选型 | 说明 |
|------|------|------|
| 数据库 | **Supabase Postgres** | 免费额度够个人产品起步，托管免运维 |
| 认证 | **Supabase Auth** | 邮箱 + GitHub/微信 OAuth，自带 JWT |
| 文件存储 | Supabase Storage | 回忆图片上传（远期） |
| AI 代理 | **Supabase Edge Function**（Deno）或 Vercel Serverless | 持有 DeepSeek API Key，校验 JWT，限流 |
| 部署 | 现有 GitHub Pages + CI | 前端不变，API 走 Supabase 域名 |

### 7.3 数据同步策略（Local-first）

```
原则：本地为读写主源（秒开秒存），云端为最终一致性副本。

写入：本地 store 变更 → 更新 localStorage + updatedAt → 队列化推送 Supabase
读取：启动时先读 localStorage（秒开）→ 后台拉取云端增量 → 合并
冲突：按 updatedAt 后写优先（last-write-wins），升级版可做字段级合并
离线：无网时仅写本地队列，恢复后自动补推（supabase-js 自带重试）
```

### 7.4 数据模型映射

```
AppState（localStorage）          Supabase 表
─────────────────────            ─────────────────────────────
version                          profiles.schema_version
visitedCities ────────────────▶  cities（user_id, payload jsonb）
scenicSpots ──────────────────▶  spots（user_id, payload jsonb）
memories ─────────────────────▶  memories（user_id, payload jsonb）
                                  + sync_meta（user_id, last_synced_at）
```

> 采用 **jsonb 存整条 payload** 而非逐列拆表：前端类型定义零改动、迁移成本低，符合「个人数据量小」的实际情况；索引只需 `user_id + updated_at`。

### 7.5 前端工程化补强

- PWA：`vite-plugin-pwa`（Phase 4.6）
- 环境变量：`.env` 管理 Supabase URL/anon key（公开安全），API Key 只放 Edge Function 环境变量
- 构建产物验证：本地 `npm run build` + `vite preview` 全流程冒烟（当前 `dist/` 未验证）
- 测试：Vitest 覆盖 store 与统计聚合函数（AI 解读的正确性依赖前端聚合的准确性）
- git 基线：**首件事提交当前脚手架**，建立可回滚基线（当前仓库 0 提交）

---

## 8. 验收标准与 KPI

### 8.1 产品级验收（「可以面向用户」的门槛）

1. **数据安全**：换设备/清缓存后数据可恢复，导出格式开放。
2. **核心链路完整**：地图 / 景点 / 回忆 / AI 助手四大模块无阻断性 Bug。
3. **AI 可用**：行程规划与年度报告生成成功率 ≥ 90%，无幻觉数字（统计来自前端计算）。
4. **合规**：AI 内容有标识、有审核、有删除通道。
5. **体验**：移动端可用（PWA 可安装），首屏加载 < 3s（本地优先策略保证）。

### 8.2 KPI 指标体系

| 阶段 | 指标 | 目标值 |
|------|------|--------|
| Phase 4 | 注册转化（访问→注册） | ≥ 10% |
| Phase 4 | 云同步成功率 | ≥ 99% |
| Phase 5 | AI 功能周使用率（活跃用户中） | ≥ 40% |
| Phase 5 | AI 生成采纳率（行程/标签） | ≥ 60% |
| Phase 5 | 年度报告分享率 | ≥ 30% |
| Phase 6 | 分享带来的新用户占比 | ≥ 20% |
| 全周期 | 次周留存 | ≥ 25% |

---

## 9. 风险与对策

| # | 风险 | 等级 | 对策 |
|---|------|------|------|
| 1 | localStorage 数据在升级时损坏 | 高 | 4.8 引入 schemaVersion + 迁移 + 双份冗余存储 |
| 2 | AI API 不稳定/超时 | 高 | 流式返回 + 30s 超时降级 + 多模型备用 |
| 3 | LLM 幻觉污染用户数据 | 高 | 前端精确计算 + AI 内容全部可拒绝 + 标注来源 |
| 4 | Supabase 免费额度超限 | 中 | 个人数据量极小，jsonb 单行存储足够；设置用量告警 |
| 5 | GeoJSON 数据源失效 | 中 | 4.5 本地打包 + 多源降级 |
| 6 | 合规风险（AI 内容） | 中 | 明示标识 + 内容审核 API + 用户删除通道 |
| 7 | API Key 泄露 | 高 | Key 仅在 Edge Function 环境变量，前端零接触 |
| 8 | 范围蔓延（AI 功能做太多） | 中 | 严格按 5.2-5.4 规格收敛，P2 项一律后置 |

---

## 10. 下一步行动

| # | 行动 | 负责人 | 时间 |
|---|------|--------|------|
| 1 | 评审本计划书，确认范围与优先级 | 产品/技术 | 本周 |
| 2 | **修复生产构建（1.4 节）+ 提交当前脚手架为 git 基线**（当前 0 提交） | 技术 | 立即 |
| 3 | 注册 Supabase 项目 + 配置 Auth/表结构（Phase 4.1-4.2 先行） | 技术 | 下周 |
| 4 | 申请 DeepSeek API Key + 搭建 Edge Function 骨架 | 技术 | Phase 5 前 |
| 5 | 高德开放平台申请地理编码 Key（个人开发者免费） | 技术 | Phase 4.4 前 |

---

*本文档由产品经理视角撰写，技术方案为建议性，落地时可结合实际调整。*
*规划日期：2026-08-15 ｜ 版本 v1.0*
